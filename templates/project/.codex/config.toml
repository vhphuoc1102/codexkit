model = "gpt-5.4"
model_reasoning_summary = "auto"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# CodexKit: compact recovery prompt
compact_prompt = """
MANDATORY: CodexKit context compaction recovery.

STOP. Before doing anything else:
1. Read AGENTS.md completely.
2. If present, run node .codex/codexkit_status.mjs --json.
3. If present, read .codexkit/HANDOFF.json and .codexkit/state.json.
4. Re-open the current Bead or task before running implementation commands.
5. Check active reservations before writing:
   node .codex/codexkit_reservations.mjs list --active-only --json
6. Check the current worktree state with git status before resuming.

After completing these steps, briefly confirm what context you restored and only then continue.
"""

[features]
codex_hooks = true

[agents]
max_threads = 6
max_depth = 1

################################################################################
# MCP servers
# Docs:
# - https://developers.openai.com/codex/mcp
# - https://developers.openai.com/codex/config-reference
################################################################################
[mcp_servers]

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"
# For higher rate limits and private repositories, add your API key:
# http_headers = { "CONTEXT7_API_KEY" = "YOUR_API_KEY" }
# startup_timeout_sec = 10.0
# tool_timeout_sec = 60.0

# Example: OpenAI developer docs over Streamable HTTP
# [mcp_servers.openai_developer_docs]
# url = "https://developers.openai.com/mcp"
# startup_timeout_sec = 10.0
# tool_timeout_sec = 60.0

# Example: local stdio MCP server
# [mcp_servers.local_docs]
# command = "docs-server"
# args = ["--port", "4000"]
# cwd = "."
# startup_timeout_sec = 10.0
# tool_timeout_sec = 60.0

# Example: MySQL MCP server (disabled until you uncomment and configure it)
# [mcp_servers.mysql]
# command = "npx"
# args = ["-y", "@benborla29/mcp-server-mysql"]
# env_vars = [
#   "MYSQL_HOST",
#   "MYSQL_PORT",
#   "MYSQL_USER",
#   "MYSQL_PASS",
#   "MYSQL_DB",
#   "MYSQL_SOCKET_PATH",
#   "MYSQL_CONNECTION_STRING",
#   "ALLOW_INSERT_OPERATION",
#   "ALLOW_UPDATE_OPERATION",
#   "ALLOW_DELETE_OPERATION",
#   "ALLOW_DDL_OPERATION",
#   "MYSQL_DISABLE_READ_ONLY_TRANSACTIONS",
#   "MYSQL_SSL",
#   "MYSQL_SSL_CA",
#   "MYSQL_SSL_CERT",
#   "MYSQL_SSL_KEY"
# ]
# startup_timeout_sec = 20.0
# tool_timeout_sec = 60.0
#
# Minimal read-only example:
# export MYSQL_HOST=127.0.0.1
# export MYSQL_PORT=3306
# export MYSQL_USER=root
# export MYSQL_PASS=your_password
# export MYSQL_DB=your_database
